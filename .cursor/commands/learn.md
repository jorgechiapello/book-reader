# Learning

Follow the **learning** skill. Analyze the last 10 messages (or N if specified), find gaps (agent output vs user corrections), and output them under `## Gaps`.

For persistence, use the appropriate sub-skill under `learning/` (create-rules, create-command, create-subagents, create-skills). Ask before writing to AGENTS.md or creating files.
